class Network:
    def __init__(self, name):
        self.name = name
        self.entities = []
        self.signals = {}

    def add_entity(self, entitity):
        self.entities.append(entity)

    def add_signal(self, signal, amount):
        for entity in self.entities:
            for 
